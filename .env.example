# Copy to .env.local for local dev.
# The backend URL the frontend calls — must be reachable from the browser
# (not the Docker-internal `backend` hostname).
NEXT_PUBLIC_API_URL=http://localhost:3001
